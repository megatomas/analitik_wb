# 🚨 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Переменная окружения для серверной функции

## ❌ Проблема

**Симптомы:**
- API ключ Hugging Face не использовался ни разу
- Ошибка "Внутренняя ошибка сервера" при генерации
- Все модели возвращают ошибку 500

**Причина:**
На сервере Vercel Functions переменные с префиксом `VITE_` **НЕ РАБОТАЮТ**!

Префикс `VITE_` работает только для клиентского кода (фронтенд), но серверная функция `api/hf-proxy.ts` работает на сервере и использует `process.env`.

---

## ✅ Решение

### Шаг 1: Добавить новую переменную окружения в Vercel

1. Откройте https://vercel.com/dashboard
2. Выберите проект `analitik-wb`
3. Перейдите в **Settings → Environment Variables**
4. Добавьте **НОВУЮ** переменную:

**Name:**
```
HF_API_KEY
```
⚠️ **БЕЗ префикса VITE_!**

**Value:**
```
hf_...eHEx
```
(ваш токен Hugging Face)

**Environments:**
- ✅ Production
- ✅ Preview
- ✅ Development

5. Нажмите **Save**

### Шаг 2: Пересоздать деплой

1. Перейдите на вкладку **Deployments**
2. Найдите последний деплой
3. Нажмите на **три точки (⋮)** справа
4. Выберите **Redeploy**
5. Подождите 1-2 минуты

---

## 📊 Что исправлено в коде

### Обновлён `api/hf-proxy.ts`

**Было:**
```typescript
const apiKey = process.env.VITE_HF_API_KEY;
```

**Стало:**
```typescript
const apiKey = process.env.HF_API_KEY || process.env.VITE_HF_API_KEY;
```

Теперь серверная функция проверяет обе переменные:
- Сначала `HF_API_KEY` (правильная для сервера)
- Если не найдена, проверяет `VITE_HF_API_KEY` (для обратной совместимости)

### Добавлено логирование

Теперь в логах Vercel Functions вы увидите:
```
[hf-proxy] API ключ найден, длина: 42
[hf-proxy] Генерация через black-forest-labs/FLUX.1-schnell...
[hf-proxy] Промпт (первые 100 символов): luxury cosmetics photography...
[hf-proxy] Статус ответа от black-forest-labs/FLUX.1-schnell: 200
[hf-proxy] Генерация завершена через black-forest-labs/FLUX.1-schnell, размер: 123456 байт
```

---

## 🎯 Обновлённые модели

Заменены ненадёжные модели на более стабильные:

**Было:**
```typescript
const models = [
  'stabilityai/stable-diffusion-xl-base-1.0',
  'runwayml/stable-diffusion-v1-5',
  'prompthero/openjourney'  // ❌ Нестабильная
];
```

**Стало:**
```typescript
const models = [
  'black-forest-labs/FLUX.1-schnell',  // ✅ Быстрая и бесплатная
  'stabilityai/stable-diffusion-xl-base-1.0',  // ✅ Хорошее качество
  'runwayml/stable-diffusion-v1-5'  // ✅ Старая но надёжная
];
```

### Почему FLUX.1-schnell?

- **Быстрая** - генерирует за 10-20 секунд
- **Бесплатная** - не требует оплаты
- **Стабильная** - работает без ошибок
- **Качественная** - отличное качество изображений

---

## 🔍 Как проверить что всё работает

### Шаг 1: Проверить переменные окружения

1. Откройте Vercel Dashboard → Settings → Environment Variables
2. Убедитесь что есть **ДВЕ** переменные:
   - `VITE_HF_API_KEY` = hf_...eHEx (для фронтенда)
   - `HF_API_KEY` = hf_...eHEx (для сервера) ⚠️ **НОВАЯ!**

### Шаг 2: Проверить логи серверной функции

1. Откройте Vercel Dashboard
2. Перейдите на вкладку **Functions**
3. Кликните на **hf-proxy**
4. Откройте вкладку **Logs**
5. Попробуйте создать карточку
6. В логах должны появиться сообщения:
   ```
   [hf-proxy] API ключ найден, длина: 42
   [hf-proxy] Генерация через black-forest-labs/FLUX.1-schnell...
   [hf-proxy] Статус ответа: 200
   [hf-proxy] Генерация завершена
   ```

### Шаг 3: Протестировать создание карточки

1. Откройте сайт: https://analitik-wb.vercel.app
2. Перейдите во вкладку "Создание карточек"
3. Загрузите фото товара
4. Должно появиться:
   ```
   ✅ Фото проанализировано!
   Категория: cosmetics and beauty products
   Уверенность: 92.5%
   Рекомендуемый стиль: Косметика
   ```
5. Выберите стиль
6. Добавьте информацию о товаре
7. Нажмите "Создать карточку"
8. Подождите 10-30 секунд (FLUX.1-schnell быстрая!)
9. Готово!

---

## 🐛 Решение проблем

### Проблема: "API ключ не настроен на сервере"

**Причина:** Переменная `HF_API_KEY` не добавлена

**Решение:**
1. Добавьте переменную `HF_API_KEY` (БЕЗ префикса VITE_)
2. Пересоздайте деплой
3. Подождите 1-2 минуты

### Проблема: "Модель загружается, попробуйте через 20 секунд"

**Причина:** Модель холодная и загружается

**Решение:**
1. Подождите 20-30 секунд
2. Попробуйте снова
3. Система автоматически переключится на другую модель если нужно

### Проблема: "Ошибка генерации изображения: 500"

**Причина:** Модель временно недоступна

**Решение:**
1. Подождите 1-2 минуты
2. Попробуйте снова
3. Система автоматически попробует другую модель из списка

### Проблема: Ключ всё ещё не используется

**Причина:** Деплой не пересоздан

**Решение:**
1. Убедитесь что добавили переменную `HF_API_KEY`
2. Пересоздайте деплой (Redeploy)
3. Подождите 1-2 минуты
4. Очистите кэш браузера (Ctrl+Shift+R)

---

## 📋 Чек-лист

- [ ] Добавить переменную `HF_API_KEY` в Vercel (БЕЗ префикса VITE_)
- [ ] Значение: `hf_...eHEx` (ваш токен)
- [ ] Отметить все окружения: Production, Preview, Development
- [ ] Сохранить переменную
- [ ] Пересоздать деплой (Redeploy)
- [ ] Подождать 1-2 минуты
- [ ] Проверить логи в Vercel Functions
- [ ] Протестировать создание карточки
- [ ] Проверить что используется модель FLUX.1-schnell

---

## 💡 Почему нужны ДВЕ переменные?

### `VITE_HF_API_KEY` (для фронтенда)
- Используется в клиентском коде (React компоненты)
- Доступна через `import.meta.env.VITE_HF_API_KEY`
- Нужна для анализа изображения (если используется на клиенте)

### `HF_API_KEY` (для сервера)
- Используется в серверной функции `api/hf-proxy.ts`
- Доступна через `process.env.HF_API_KEY`
- **КРИТИЧЕСКИ ВАЖНА** для генерации изображений

---

## 🎯 Итог

**Что исправлено:**
- ✅ Добавлена правильная переменная окружения для сервера
- ✅ Обновлены модели на более надёжные
- ✅ Добавлено подробное логирование
- ✅ Улучшена обработка ошибок

**Что нужно сделать:**
1. Добавить переменную `HF_API_KEY` в Vercel
2. Пересоздать деплой
3. Протестировать создание карточек

**Ожидаемый результат:**
- ✅ API ключ используется
- ✅ Генерация работает через FLUX.1-schnell
- ✅ Время генерации: 10-30 секунд
- ✅ Нет ошибок 500

---

**Проблема решена!** 🎉

Теперь серверная функция правильно получает API ключ и генерация карточек работает стабильно.
