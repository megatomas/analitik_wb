# 🚀 Быстрый старт: Как заставить WB Analytics работать

## ⚡ Что нужно сделать прямо сейчас (5 минут)

### Шаг 1: Запушьте код на GitHub

Откройте терминал в папке проекта и выполните:

```bash
git add .
git commit -m "Add Vercel serverless proxy for WB API"
git push
```

### Шаг 2: Дождитесь деплоя на Vercel (2-3 минуты)

Vercel автоматически:
- Задеплоит фронтенд (React)
- Развернёт серверную функцию `api/wb-proxy.ts`
- Настроит маршрутизацию через `vercel.json`

### Шаг 3: Проверьте что серверная функция работает

Откройте в браузере:
```
https://analitik-wb.vercel.app/api/wb-proxy
```

**Должно появиться:**
```json
{"error":"API ключ не предоставлен"}
```

✅ Если видите это — **всё работает!** Переходите к шагу 4.

❌ Если видите 404 — функция не развернулась. Проверьте логи в Vercel Dashboard.

### Шаг 4: Обновите страницу сайта

Откройте:
```
https://analitik-wb.vercel.app/
```

Обновите страницу (Ctrl+F5 или Cmd+Shift+R).

Теперь данные должны загружаться из вашего кабинета Wildberries! 🎉

---

## 🔍 Как проверить что всё работает

### Тест 1: Проверьте функцию

Откройте: `https://analitik-wb.vercel.app/api/wb-proxy`

Должен увидеть: `{"error":"API ключ не предоставлен"}`

### Тест 2: Проверьте консоль браузера

1. Откройте сайт
2. Нажмите F12
3. Перейдите во вкладку **Console**
4. Должны увидеть логи:
   ```
   [WB API] Запрос: /api/v1/supplier/sales?dateFrom=...
   [WB API] Статус: 200
   ```

### Тест 3: Проверьте Network tab

1. F12 → вкладка **Network**
2. Обновите страницу
3. Найдите запрос к `/api/wb-proxy`
4. Статус должен быть **200**
5. Response должен содержать реальные данные из WB

---

## ❌ Если не работает

### Проблема: Ошибка 404

**Причина:** Серверная функция не развернулась

**Решение:**

1. Проверьте что файл `api/wb-proxy.ts` есть в репозитории
2. Проверьте что `vercel.json` есть в корне проекта
3. Проверьте что `@vercel/node` установлен:
   ```bash
   npm install @vercel/node
   git add package.json package-lock.json
   git commit -m "Add @vercel/node dependency"
   git push
   ```
4. Пересоздайте деплой:
   - Vercel Dashboard → ваш проект
   - Вкладка **Deployments**
   - Нажмите `...` на последнем деплое
   - Выберите **Redeploy**

### Проблема: Ошибка 429

**Причина:** Превышен лимит WB API

**Решение:**
- Подождите 1-2 минуты
- Обновите страницу
- Данные кэшируются на 3 минуты

### Проблема: Ошибка 401

**Причина:** Неверный API-ключ

**Решение:**
1. Откройте настройки профиля (клик на аватар)
2. Проверьте API-ключ
3. Убедитесь что ключ начинается с `eyJ...`
4. Создайте новый ключ в кабинете WB если нужно

### Проблема: Ошибка 403

**Причина:** Нет прав доступа

**Решение:**
1. Создайте новый API-ключ в кабинете WB
2. Отметьте категории: **Статистика** + **Аналитика**
3. Уровень доступа: **Только чтение**
4. Вставьте новый ключ в настройки профиля

---

## 📋 Структура проекта

Убедитесь что файлы лежат правильно:

```
ваш-репозиторий/
├── api/
│   └── wb-proxy.ts              ← Серверная функция (ВАЖНО!)
├── src/
│   ├── components/
│   │   ├── Dashboard.tsx
│   │   ├── StockRecommendations.tsx
│   │   └── ...
│   ├── services/
│   │   └── wbApi.ts             ← Клиентский код для запросов
│   └── ...
├── vercel.json                  ← Конфигурация Vercel (ВАЖНО!)
├── package.json
└── ...
```

---

## 🎯 Что изменилось

### До:
- Прямые запросы из браузера к WB API
- Блокировка из-за CORS
- Ошибка 429 или Failed to fetch

### После:
```
Браузер → /api/wb-proxy (Vercel Function) → WB API
                ↓
         Серверный прокси
         (обходит CORS)
```

✅ Запросы идут через серверный прокси  
✅ CORS не блокирует  
✅ API-ключ не светится в браузере  
✅ Кэширование на сервере  

---

## 💡 Важные моменты

### 1. Серверные функции Vercel
- Разворачиваются автоматически при push
- Находятся в папке `api/`
- Работают как обычный Node.js код
- Имеют доступ к `req` и `res`

### 2. Кэширование
- Данные кэшируются на 3 минуты
- Снижает нагрузку на WB API
- Ускоряет загрузку страницы

### 3. Безопасность
- API-ключ передаётся только через HTTPS
- Не сохраняется в браузере (только в памяти)
- Серверный прокси добавляет ключ при запросе к WB

---

## 🆘 Если ничего не помогает

### Шаг 1: Проверьте логи Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `analitik-wb`
3. Вкладка **Functions**
4. Кликните на `wb-proxy`
5. Вкладка **Logs**
6. Попробуйте загрузить данные на сайте
7. В логах должны появиться запросы

### Шаг 2: Проверьте Network tab

1. F12 → вкладка **Network**
2. Обновите страницу
3. Найдите запрос к `/api/wb-proxy`
4. Посмотрите:
   - Status Code
   - Request Headers
   - Response

### Шаг 3: Очистите кэш

```bash
# В браузере
Ctrl+Shift+Delete → Очистить кэш

# Или жёсткое обновление
Ctrl+F5 (Windows/Linux)
Cmd+Shift+R (Mac)
```

### Шаг 4: Пересоздайте деплой

1. Vercel Dashboard → ваш проект
2. Вкладка **Deployments**
3. Нажмите `...` на последнем деплое
4. Выберите **Redeploy**
5. Подождите 2-3 минуты

---

## ✅ Итого

**Что нужно сделать:**
1. ✅ Запушить код на GitHub
2. ✅ Дождаться деплоя (2-3 минуты)
3. ✅ Проверить что `/api/wb-proxy` работает
4. ✅ Обновить страницу сайта

**Что должно работать:**
- ✅ Реальные данные из WB API
- ✅ Продажи за вчера/неделю/месяц
- ✅ Остатки и рекомендации
- ✅ Кэширование на 3 минуты

**Если не работает:**
- Проверьте логи в Vercel Dashboard
- Проверьте Network tab (F12)
- Пересоздайте деплой
- Очистите кэш браузера

---

## 📞 Поддержка

Если ничего не помогло:
- **Email:** support@wb-analytics.pro
- **Telegram:** @wb_analytics_support

Пришлите:
1. Скриншот ошибки
2. Скриншот логов Vercel Functions
3. Скриншот Network tab (F12)
4. URL вашего сайта

---

**Удачи! 🚀**
